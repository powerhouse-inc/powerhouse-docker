services:
    connect:
        restart: unless-stopped
        image: ghcr.io/powerhouse-inc/connect:develop
        environment:
            PORT: 3000
            BASE_PATH: "/"
        ports:
            - ${CONNECT_PORT}:3000
